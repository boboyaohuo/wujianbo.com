export default {
  primaryColor: '#1890ff', // 默认主题颜色
  // vue-ls 配置信息
  storageOptions: {
    namespace: 'bo_', // key 前缀
    name: '$ls', // 名称变量 this.[$ls],
    storage: 'local' // storage name session, local, memory
  }
}
